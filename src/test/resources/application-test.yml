spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coupon_db?rewriteBatchedStatements=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: coupon
    password: coupon1234

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  batch:
    job:
      enabled: false

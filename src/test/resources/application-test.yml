spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${DB_NAME:coupon_db_test}?rewriteBatchedStatements=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&forceConnectionTimeZoneToSession=true
    username: coupon
    password: coupon1234

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  batch:
    job:
      enabled: false

coupon:
  kafka:
    consumer:
      enabled: false

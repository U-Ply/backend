apiVersion: 1

providers:
  - name: 'uply'
    orgId: 1
    folder: 'U-Ply'
    type: file
    disableDeletion: false
    # 파일을 고치면 재기동 없이 10초 안에 반영된다.
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
